/* global module */

/**
 * Obacht Game Server Options
 * @type {object}
 */
var options = {
    maxPlayers: 2,
    maxRooms: 9999,
    defaultPort: 8080 // Use Console Argument if there
};

module.exports = options;
